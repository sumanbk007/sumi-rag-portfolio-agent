# URL of your deployed FastAPI backend (Render). For local dev, leave as is
# if you're running the backend locally on port 8000.
NEXT_PUBLIC_API_URL=http://localhost:8000

# After deploying the backend to Render, set this in Vercel's dashboard to:
# NEXT_PUBLIC_API_URL=https://your-backend.onrender.com
